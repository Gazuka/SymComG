<table class="table table-bordered" width="100%" cellspacing="0">
    {# <thead>
        <tr>
            {% for nomColonne in nomColonnes %}
                <th>{{ nomColonne }}</th>
            {% endfor %}
        </tr>
    </thead>     #}
    <tbody>
        {{ tbody }}
    </tboby>
</table>