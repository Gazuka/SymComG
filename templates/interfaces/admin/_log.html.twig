<!DOCTYPE html>
<html lang="en">
<head>      
    <title>Connexion à l'administration</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap-4-6/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/symcom4.css') }}">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
    <style>
        body {
            display: flex;
            justify-content: center;
            height: 100vh;
        }
        .card {
            margin-top: 10%;
        }
    </style>
</head>
<body>
    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-header">
                <h4 class="card-title">
                    <i class="fas fa-lock"></i>
                    Connexion à l'administration
                </h4>
            </div>
            <div class="card-body">
                {% if hasError %}
                    <div class="alert alert-warning">
                        <p>Les informations que vous avez fournies sont incorrectes.</p>
                    </div>
                {% endif %}
                <form method="post">
                    <div class="form-group">
                        <label for="_username">Pseudo</label>
                        <input type="text" class="form-control" placeholder="Pseudo..." name="_username">
                        <label for="_password">Mot de passe</label>
                        <input type="password" class="form-control" placeholder="Mot de passe..." name="_password">
                    </div>
                    <div class="form-group">
                        <button id='login_connexion' class="btn btn-primary"><i class="fas fa-lock-open"></i> Connexion !</button>
                        <a href="{{ path('site_accueil')}}" class="btn btn-link">
                            <i class="fas fa-arrow-circle-left"></i>
                            Retour au site
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>